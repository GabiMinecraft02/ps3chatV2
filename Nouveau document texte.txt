je veux faire un truc ca va être ulra ambitieux j'en ai marre du json doonc je vais créer jdat le language pour stocker les données parfait plus clair avec du cryptage complet dans un shell interactif
jdat n’est PAS un langage de programmation.
JDAT stocke uniquement des données, organisées et chiffrables
Le “code” dont je parle n’est pas du code exécutable normal, c’est juste une manière de stocker des instructions ou des métadonnées dans le fichier, toujours sous forme de données.
Donc tout reste dans () et {}, tout est lisible / modifiable / chiffrable.
Il n’y a aucune exécution de code, juste un shell qui permet de lire, modifier, chiffrer, déchiffrer et sauvegarder.
si je parle de python c'est qu'on va faire un module pour les lire

exemple de code:
({<ceci est un commentaire>})

(n:bob l:lien t:1 {
nom: bob
adresse mail: bob@exemple.com
mot de passe: 12345ABCDE._.
}
)

(n:code l:python t:2{
from flask import Flask, render_template, send_from_directory
import os

app = Flask(__name__)

# Route principale
@app.route("/")
def home():
    return render_template("index.html")

# Route pour robots.txt
@app.route("/robots.txt")
def robots():
    return send_from_directory('.', 'robots.txt')

# Route pour sitemap.xml
@app.route("/sitemap.xml")
def sitemap():
    return send_from_directory('.', 'sitemap.xml')

# Route pour fichier Google verification
@app.route("/googlea128813747473c36.html")
def google_verification():
    return send_from_directory('.', 'googlea128813747473c36.html')

if __name__ == "__main__":
    port = int(os.environ.get("PORT", 5000))
    app.run(host="0.0.0.0", port=port)
)}

n c'est nom le code s'en contre fiche l c'est link c'est ce qui permet au code de trouver une section t c'est type ya 1 (comme json) et 2 (comme l'autre base stockage de code en dur et manque un truc >code)}>(la on met un petit truc CA DEVIENT PAS DE LA PROGRAMATION CEST GENRE CREER ES FICHIERS LES OUVRIR MODIFIER...)<({code<

(maintenant disons que je ne veux pas que l'on voit le mot de passede bobje tape donc jdat shell open je fait open C:\Users\exemple\desktop\compte.jdat ensuite help je voit une commande crypt je choisis alors le link lien et mot de passe je crypte et ca créeun env qui contient le code de décryptage si ta des questions surtout pose)

je code l'exe je prend une pause en faisant le module on aura juste 2 trucs

import JdatEditor as jdat

2ème chose

def jdat.action:
	with jdat.action ('#chemin d'accès"):
		("acton'read', 'edit") # les 2 actions possibles




un site web pour créer des projets web
-------------------
header
-------------------
select language:
(on aura des cases a cocher python javascript java C...)

select type: (pour python java...)
(avec les modules par exemple pour python Flask Django Webenginepy...)

puis on aura la structure et dans chaque dossier on aura un petit + avec folder file
boutton create project qui va télécharger le dossier du projet
-------------------
footer
-------------------
		





structure:
codes/
|-flask_app.txt (app.py flask)
|-django_manage.txt (manage.py django)
|-webenginepy_engine.txt (engine.py webenginepy)
|-google_search_console/
	|-robots_txt.txt
	|-stemap_xml.txt